plugins {
    id 'net.neoforged.moddev' version '2.0.139'
}

version = "0.1.0-alpha"
group = "com.wheatbread11.komugimod"

neoForge {
    version = '21.1.218'

    runs {
        client {
            client()
        }
        server {
            server()
        }
        data {
            data()
        }
    }

    parchment {
        minecraftVersion = '1.21.1'
        mappingsVersion = '2024.11.17'
    }

    mods {
        "komugimod" {
            sourceSet(sourceSets.main)
        }
    }
}